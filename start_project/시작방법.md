# 1. 프로젝트 파일을 서버로 전송 (로컬에서)
scp -r ./nestjs-react-sqlite_1 user@서버IP:/tmp/

# 2. 서버 접속 후 실행
ssh user@서버IP
cd /tmp/nestjs-react-sqlite_1
chmod +x deploy.sh
sudo bash deploy.sh

# 1. 프로젝트 파일을 서버로 전송 (로컬에서)
scp -r ./nestjs-react-sqlite_1 user@서버IP:/tmp/

# 2. 서버 접속 후 실행
ssh user@서버IP
cd /tmp/nestjs-react-sqlite_1
chmod +x deploy.sh
sudo bash deploy.sh
